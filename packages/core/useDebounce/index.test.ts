import { useDebounce } from '../useDebounce'

describe('useDebounce', () => {
  it('should be defined', () => {
    expect(useDebounce).toBeDefined()
  })

  it('should be debounce multiple calls', () => {
  })
})
